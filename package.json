{
  "dependencies": {
    "cypress": "^13.13.2"
  },
  "scripts": {
    "browser:chrome": "npx cypress run --browser=chrome --headless --spec cypress/e2e/tests --reporter mocha-allure-reporter",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open  allure-report"
  },
  "devDependencies": {
    "allure-commandline": "^2.30.0",
    "cypress": "^13.13.2",
    "mocha-allure-reporter": "^1.4.0"
  }
}
